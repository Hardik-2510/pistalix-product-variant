query CartTransformRunInput {
  cart {
    lines {
      id
      addonPrice: attribute(key: "_addon_price") {
        value
      }
      feeVariantId: attribute(key: "_fee_variant_id") {
        value
      }
      merchandise {
        ... on ProductVariant {
          id
        }
      }
    }
  }
}
